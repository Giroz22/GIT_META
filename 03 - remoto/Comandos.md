### Comando para realizar la conexion remota

$ git remote add origin < URL del repositorio >

### Comando para listar la conexion remota

$ git remote -v

### Comando oara eliminar una conexion remota

$ git remote remove < URL del repositorio >
